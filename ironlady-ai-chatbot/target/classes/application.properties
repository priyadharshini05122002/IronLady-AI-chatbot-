# Application name
spring.application.name=ironlady-ai-chatbot

# Server port (optional, but recommended)
server.port=8080

# Logging level (optional – helps during debugging)
logging.level.org.springframework=INFO

# Disable Thymeleaf cache during development
spring.thymeleaf.cache=True

# Context path (optional)
# server.servlet.context-path=/ironlady
